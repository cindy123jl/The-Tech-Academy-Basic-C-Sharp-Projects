using System;
namespace ClassSubmissionAssignment
{
    public class Operations
    {   //our method with arguments
        public void methodOne(out int x, out int y, out int total)
        {
            
            x = 0;//assigning x to 0 
            y = 4;// y will be constant at the value of 4
            total = x / y;//math operation
          
        }
    }
}
